import styled from "styled-components";

export const Container = styled.div`
    width: 90vw;
    padding: 20px;
    display: flex; 
    justify-content: space-between;
    img{
        cursor: pointer;
    }
`